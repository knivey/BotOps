#you should configure this and rename it to main.conf
mysql {
  user mysqluser
  pass mysqlpass
  host localhost
  database botsdatabse
}

botnet {
  host 0
  port 45020
  timeout 15
}

irc {
  connect_timeout 15
  ping_timeout 180
  authserv "as auth username password"
  wraplen 400
  max_buffer 9001
}
